//! gRPC auth service implementation

/// Auth service implementation placeholder
#[derive(Debug, Clone)]
pub struct AuthServiceImpl;

impl AuthServiceImpl {
    /// Create new auth service
    pub fn new() -> Self {
        Self
    }
}

impl Default for AuthServiceImpl {
    fn default() -> Self {
        Self::new()
    }
}
