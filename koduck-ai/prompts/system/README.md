# System Prompts

This directory stores prompt fragments that are injected by `koduck-ai` at runtime.

- `koduck-v1-lite.md`: the default always-on Koduck behavior contract. It must be included in every chat turn as part of the system prompt.
