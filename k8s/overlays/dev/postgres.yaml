apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: koduck
spec:
  volumeClaimTemplates:
    - metadata:
        name: postgres-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 128Mi
        storageClassName: hostpath
