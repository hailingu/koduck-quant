apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: koduck-dev

resources:
  - ../../base/namespace.yaml
  - ../../base/apisix.yaml

namePrefix: dev-

commonLabels:
  environment: dev

patchesStrategicMerge:
  - resources.yaml
