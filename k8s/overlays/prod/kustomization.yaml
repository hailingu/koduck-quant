apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: koduck-prod

resources:
  - ../../base/namespace.yaml
  - ../../base/apisix.yaml

namePrefix: prod-

commonLabels:
  environment: prod

patchesStrategicMerge:
  - resources.yaml
  - replicas.yaml
