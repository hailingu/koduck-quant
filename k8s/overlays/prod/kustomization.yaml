apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: koduck-prod

resources:
  - ./namespace.yaml
  - ./apisix.yaml

namePrefix: prod-

labels:
  - pairs:
      environment: prod
    includeSelectors: true
