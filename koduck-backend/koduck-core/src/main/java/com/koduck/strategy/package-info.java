/**
 * Root namespace for the strategy module.
 *
 * <p>This package intentionally remains empty in the current codebase.
 * The strategy module's classes are organised by layer in sub-packages:</p>
 *
 * <ul>
 *   <li>{@code com.koduck.controller.strategy} — REST controllers</li>
 *   <li>{@code com.koduck.dto.strategy} — request/response DTOs</li>
 *   <li>{@code com.koduck.entity.strategy} — JPA entities</li>
 *   <li>{@code com.koduck.repository.strategy} — data access layer</li>
 *   <li>{@code com.koduck.service.impl.strategy} — service implementations</li>
 * </ul>
 *
 * <p>Future core strategy-engine classes (e.g. {@code StrategyEngine},
 * {@code StrategyEvaluator}) may be placed directly in this package when
 * they are introduced.</p>
 */
package com.koduck.strategy;
